service:
  name: needs-typescript

plugins:
  - serverless-webpack

provider:
  name: aws
  runtime: nodejs10.x

functions:
  hello:
    handler: functions/handler.hello
    events:
      - http:
          method: get
          path: hello

resources:
  # Cognito
  - ${file(resources/cognito-user-pool.yml)}

